# Session handoff — 2026-08-28

## Authoritative accepted production code checkpoint before this handoff-doc commit

- Repository: `lukindv77/ProxyToAnyConnect`.
- Production `main`: `5811900dfbf7488bd8ac53af20348c462681eeef`.
- Tree: `e44bf16408da3abade0c0f4d04708e6fd5ccd4ac`.
- Exact-main build #616 / run `33152272544`: success through evidence smokes, restore/build, aggregate self-tests, self-contained win-x64 publish, integrity manifest, ZIP and artifact upload.
- Windows artifact `9678213447`, digest `sha256:bd31b7f143d11c56cfc6794e55760e156341ca07bdd8fcbb52691d5010e9c1e7`.
- Exact-main handoff #393 / run `33152272516`: success.
- Handoff artifact `9678172387`, digest `sha256:bef544b5997914274001b50fce35684dcdd633d44c6230de654c0769db0a77c9`.

This documentation commit intentionally moves `main`; verify its resulting exact head before continuing.

## Major engineering completed in the latest sequence

### #79 — outbound connection deadline
Configured `outboundConnectTimeoutSeconds` now bounds the entire outbound acquisition operation while preserving precedence: proxy owner cancellation remains lifecycle cancellation; VPN invalidation remains fail-closed 503 semantics; a genuine proxy-created deadline before response commitment maps to 504. CONNECT/plain HTTP post-commit rules remain intact. Closed completed after exact build #612 + handoff #391.

### #80 — client header deadline
Configured client header timeout now yields 408 when the client fails to complete its request before the deadline, before any outbound/VPN ownership. Pause/Shutdown cancellation is not misclassified as 408/500. Closed completed after production `e5c817aeebc3b29c8e19fa07423eba8221cd47d7`, exact build #614 + handoff #392.

### #85 — terminal cleanup ownership
A first terminal coordinator/host cleanup failure no longer discards the only exact VPN owner capable of retrying residual HRASCONN/private-PBK teardown. Only failed owners are retained for serialized cleanup-only retry; successful owners are not retried; runtime APIs stay permanently disposed. The real application shutdown sequence attempts every independent first-pass owner and then, only when the first runtime-host cleanup failed, performs at most one immediate second call to the same runtime host. Clean PR #86 build #615 passed; merged production `5811900dfbf7488bd8ac53af20348c462681eeef`; exact build #616 + handoff #393 passed; #85 closed completed.

## Earlier deterministic chain now accepted

Recent completed findings include #52/#53/#54/#58/#59/#62/#63/#66/#67/#70/#71/#73/#75/#77 plus earlier strict HTTP/routing/performance-test hardening. Consult live issue comments for exact lineage rather than old dev branches.

## Audit blocks with no new issue

- DNS timeout/failover is now bounded by the overall #79 outbound deadline while retaining lower-level DNS deadlines and L2TP-only routing.
- Windows VPN profile helper process execution/termination/drain path is bounded and argument-safe.
- ICMP async lifetime has a documentation uncertainty about handle-close vs pending I/O, but no concrete deterministic unsafe path was proven.
- Proxy terminal lease release does not need an independent retry layer because residual native ownership is retained below it by the #85 VPN-manager/coordinator cleanup path.

## Release-critical open boundaries

Live open issues are #2/#4/#5/#6/#7/#11/#13. Real endpoint/operator evidence remains mandatory for #2/#4/#5/#6/#7. #13 still requires representative 12–24 h exact-binary soak with traffic/reconnect/Pause/Resume/reconfigure and correlated process/native + managed resource series. #11 remains a permanent performance/memory architecture requirement. Hosted CI smoke proves tooling mechanics only.

## Immediate continuation

1. Fetch exact live `main`, tree and exact-head build/handoff after this docs commit.
2. Continue broad deterministic audits across proxy ownership/deadlines, RAS/native interop, verification parsing, DNS/cache and bounded process state.
3. For every new concrete finding: issue-first acceptance -> code/tests -> permanent Windows PR CI -> merge/rebase -> exact-main build + handoff -> exact lineage comment.
4. Preserve no-DIRECT routing, exact VPN ownership, response-commit boundaries, pooled 32 KiB transfer path, bounded memory, no forced GC and the unchanged 1.25x policy.
5. Never fabricate real Windows/L2TP or 12–24 h soak evidence.

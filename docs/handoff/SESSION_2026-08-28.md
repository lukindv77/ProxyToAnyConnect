# Session handoff — 2026-08-28

## Authoritative accepted production checkpoint before this handoff-doc commit

- Repository: `lukindv77/ProxyToAnyConnect`.
- Production `main`: `ddbdc95e3b9e7080a31c2b631da1c1f187a1f1a3`.
- Tree: `4f11a13a1ac0d1839b86671dc0b7ccae7eed0d40`.
- Exact-main build #580 / run `33132200561`: success including evidence smokes, full aggregate self-tests, self-contained win-x64 publish, integrity manifest, ZIP and artifact upload.
- Windows artifact id `9670700014`, digest `sha256:83e91fbda614aeb804fcdecfc05bf589247582143c609a453be76f5e92acd76e`.
- Exact-main handoff #375 / run `33132200498`: success.
- Handoff artifact id `9670678196`, digest `sha256:6091de65429ce10c5275a7a7ba27739b0d49cc4b635f182a27ea8f72cbb812d5`.

This documentation commit intentionally moves `main`; new chats must validate Actions on the new exact head.

## Accepted earlier in this session

### #45 — strict HTTP request line
Inbound HTTP/1.x parsing requires exactly `method SP request-target SP HTTP-version`, with exactly two ASCII SP separators. Leading/trailing/repeated SP and HTAB/VT/FF/bare-CR separator forms reject before outbound/VPN/DNS/socket ownership. #45 closed completed.

### #47 — soak duration integrity
`observedDurationSeconds` producer/validator semantics use first/last serialized sample timestamps. The existing 50 ms consistency tolerance remains unchanged; tamper/mismatch remains fail-closed. #47 closed completed.

### #6 re-audit — no duplicate patch
Current production already has canonical/reparse-safe/exact-leaf/non-recursive CustomEphemeral orphan cleanup plus Windows regressions. Managed RAS password/PSK carriers are cleared immediately after native handoff. #6 remains open for real Windows 11/L2TP acceptance and any future concrete finding.

## Accepted #49/#50 production block

### #49 — verification request-target integrity
`verification.probePath` is now accepted only as byte-exact ASCII HTTP origin-form. Valid `%HH` is required; fragments, controls, SP/HTAB, non-ASCII and malformed/lossy forms reject. `VpnConnectivityVerifier.BuildProbeRequest` independently applies the same fail-closed boundary before ASCII encoding. The existing allocation/timing guard remains and no proxy transfer hot path changed.

### #50 — verification authority identity
`verification.probeHost` is canonicalized to IDNA/A-label form and checked with explicit strict ASCII DNS LDH-label grammar after IDNA. One canonical authority is used for L2TP-bound DNS resolution, TLS `TargetHost`/SNI and HTTP `Host`. `münich.example` becomes `xn--mnich-kva.example`; `_`, empty/oversized labels and leading/trailing hyphens reject.

### Acceptance lineage

- dev validation branch `dev/issue49-probe-target` produced Windows-green source commit `1684718295944ecdb28216ae02c32365ff7b2b0c` in run `33130832271`;
- clean reconstruction `c67a29a0c82a5eb6f5bdee4e20ece39c426ac652` copied exactly four production/test blobs and excluded all dev workflow/transport history;
- permanent PR #51 changed exactly four files;
- PR build #579 / run `33131957422`: attempt 1 passed the new #49/#50 functional suites but the pre-existing DNS setup microbenchmark measured 1.30x vs the unchanged 1.25x policy; historical #11 evidence already documented runner sensitivity, so neither threshold nor production was changed; identical-head attempt 2 on a different Windows runner passed build + aggregate self-tests;
- PR #51 was rebase-merged to `ddbdc95e3b9e7080a31c2b631da1c1f187a1f1a3`;
- exact-main build #580 and handoff #375 were both green;
- #49 and #50 were updated with exact SHA/run/artifact evidence and closed completed.

The dev branch remains historical validation lineage only. Do not merge it wholesale.

## Release-critical open boundaries

Real endpoint/evidence remains mandatory for #2, #4, #5, #6 and #7. #13 still requires representative 12–24 h exact-binary soak with traffic/lifecycle churn and correlated process/native + managed resource records. #11 remains a permanent performance/memory architecture requirement. Hosted CI smoke proves tooling mechanics only, not a real VPN endpoint or leak-free multi-hour operation.

## Immediate continuation

1. Fetch exact live `main`, tree and exact-head `build`/`handoff` after this docs commit.
2. Read current handoff docs, requirements/architecture/memory docs, Windows integration/soak docs and live issue comments.
3. Continue a broad deterministic audit block; do not churn #45/#47/#49/#50 or the already-audited #6 cleanup/secret-carrier boundary without a new concrete finding.
4. For each new finding: issue-first acceptance → code/tests → permanent Windows PR CI → merge/rebase → exact-main build + handoff → concrete issue/handoff evidence.
5. Never fabricate real Windows/L2TP or 12–24 h soak evidence.

# Session handoff — 2026-08-28

## Authoritative production baseline before handoff-doc commit

- Repository: `lukindv77/ProxyToAnyConnect`.
- Production `main`: `2e56f8f76efda9047ec83f3cd0e58aee395de322`.
- Clean cumulative PR #48 accepted #47 soak duration integrity and #45 strict HTTP request-line separators.
- Permanent PR Windows build run `33097205158`: success.
- Exact-main build #577 / run `33097542082`: success including evidence smokes, full aggregate self-tests, self-contained win-x64 publish, integrity manifest, ZIP and artifact upload.
- Exact-main handoff #373 / run `33097542206`: success.
- Exact-main handoff artifact id `9657003054`, digest `sha256:a7fcf633740e12b2fa2dcde388567b7038ea48b4686a725986e0c517c40394f0`.

The handoff documentation commit after this snapshot intentionally moves `main`; new chats must validate Actions on the new exact head.

## Accepted in this session

### #45 — HTTP request line
Inbound HTTP/1.x parsing now requires exactly `method SP request-target SP HTTP-version`, with exactly two ASCII SP separators. Leading/trailing/repeated SP and HTAB/VT/FF/bare-CR separator forms are rejected before outbound/VPN/DNS/socket ownership. Parser/live no-outbound/allocation/timing regressions passed Windows CI. #45 is closed completed.

### #47 — soak duration integrity
Soak `observedDurationSeconds` producer/validator semantics were aligned to the first/last serialized sample timestamps. The validator keeps the existing 50 ms consistency tolerance; the fix does not mask mismatches by widening tolerance. Fresh evidence validates and tampered duration/timestamp evidence remains fail-closed. #47 is closed completed.

### #6 audit — no duplicate patch
Current production `EphemeralRasPhonebook` already has canonical managed session names, regular-directory/reparse checks, marker validation, exact known-leaf whitelist, lock-first ownership, non-recursive deletion and fail-safe preservation of ambiguous residue. Windows self-tests cover malformed/oversized marker, unexpected child, noncanonical names, reparse target preservation, live-owner locks and 16-cycle partial creation failure churn.

Credential lifetime was also re-audited: `RasDialer` clears `RasDialParams.SzPassword` immediately after native handoff and on native throw; `SetPreSharedKey` clears the managed `RasCredentials.SzPassword` carrier and local PSK reference in `finally`. Do not churn #6 absent a new concrete finding; it remains open for real Windows 11/L2TP acceptance.

## New audit findings / open work

### #49 — unsafe/lossy verification probe request-target
Production `verification.probePath` validates only non-empty + leading `/`, then `BuildProbeRequest` inserts it through `Encoding.ASCII`. Framing controls/spaces can alter the request and non-ASCII can silently become `?`. Acceptance requires byte-exact ASCII origin-form, valid `%HH`, rejection of fragments/controls/non-ASCII/malformed escapes, and builder-level fail-closed behavior while retaining the performance guard.

### #50 — verification IDN authority differential
Windows/.NET 10 diagnostic run `33098221153` proved `Uri.CheckHostName('münich.example') == Dns`. Production passes Unicode to L2TP DNS/TLS while HTTP Host goes through ASCII replacement, creating authority divergence. Run4 `33130656615` additionally proved `Uri.CheckHostName` accepts `bad_.example`; strict explicit DNS LDH-label grammar is therefore part of the security contract.

## #49/#50 development branch

Branch `dev/issue49-probe-target`, derived from production baseline `2e56f8f76efda9047ec83f3cd0e58aee395de322`.

Important identities:
- `.github/validation/issue49-transform.ps1` blob `8986e4461c3a2098be6a4519b1b42e9ad124c7d5`;
- `.github/validation/issue49-post-transform.ps1` blob `ef2db77e477b49cde12f63ad51f0d5a2c19d663f`;
- validation setup commit `545351d2cb7871f3b903b6242c82d494a0cde17d`;
- run7 `33130832271`: SUCCESS across transforms, full aggregate self-tests and source publish;
- bot-published validated source commit `1684718295944ecdb28216ae02c32365ff7b2b0c`.

Validation lineage:
- `33098221153`: transport failed before compile; diagnostic confirmed Unicode host classifier result `Dns`;
- `33098523188`: patch-hunk transport failure;
- `33098768040`: transform passed; compile caught missing namespace import in new test;
- `33130656615`: compile + aggregate ran; only new suites failed because `bad_.example` escaped platform classifier;
- run5/run6: YAML parse-only failures while moving strict-label transform; no production semantic verdict;
- `33130832271`: final dev Windows validation green and source publish green.

Validated source commit changes exactly four files:
1. `src/ProxyToAnyConnect/Configuration/AppOptions.cs`
2. `src/ProxyToAnyConnect/Vpn/VpnConnectivityVerifier.cs`
3. `tests/ProxyToAnyConnect.SelfTests/SettingsValidationSelfTests.cs`
4. `tests/ProxyToAnyConnect.SelfTests/VerificationProbeRequestSelfTests.cs`

Do not merge the development workflow/transport history wholesale. Reconstruct those four files cleanly from current main, run permanent PR CI, merge/rebase, then require exact-main build + handoff before closing #49/#50.

## Release-critical open boundaries

Real endpoint/evidence remains mandatory for #2, #4, #5, #6, #7; #11 remains a permanent performance/memory architecture requirement; #13 still requires representative 12–24 h exact-binary soak with traffic/lifecycle churn and correlated process/native + managed memory records. Hosted CI smoke proves tooling mechanics only, not a real VPN endpoint or leak-free multi-hour operation.

## Immediate continuation

1. Fetch exact live `main` and exact Actions after this handoff commit.
2. Read all handoff docs, requirements/architecture/memory docs, Windows integration/soak docs, and live issue comments.
3. Verify `dev/issue49-probe-target` still contains `1684718295944ecdb28216ae02c32365ff7b2b0c`; finish clean #49/#50 acceptance from only the four source/test files.
4. Require permanent Windows PR CI and exact-main `build` + `handoff` before closing #49/#50.
5. Continue broad deterministic hardening but do not duplicate accepted #45/#47/#6-audited work without a new finding.
6. Never fabricate real Windows/L2TP or 12–24 h soak evidence.

# Session transition — 2026-09-13

Live GitHub is authoritative over this snapshot.

## Current repository checkpoint

- Repository: `lukindv77/ProxyToAnyConnect`.
- Current `main`: `4a653627e0aa0a22967a077821470c133980c675`.
- Current tree: `e6928be6d0134330cf8b7637e475e69ff159cdd5`.
- The last production-changing commit is `f0763ec9337a0758c45a0add65e27d4b8f689482` (#92); two later maintenance commits temporarily added and then removed a one-time Actions cleanup workflow. The final tree is again exactly `e6928be6d0134330cf8b7637e475e69ff159cdd5`.
- Exact production-tree build #624 / run `33165692687`: success; artifact `9683511938`, digest `sha256:38bde53b760ceeb045d985058d7c5627f7275d189422c70fb277a45b5cab8247`.
- Exact production-tree handoff #397 / run `33165692716`: success; artifact `9683470928`, digest `sha256:2bd3366dd24a68e3dd3911438055ade9e9e51f13d235b0bb7f0b1c832e45368c`.

## Latest completed hardening

- #88 verification HTTP grammar: accepted and closed completed.
- #89 DNS exact-owner CNAME RRset ambiguity: accepted and closed completed.
- #92 DNS response OPCODE and exact-owner A RDATA grammar: accepted and closed completed; production commit `f0763ec9337a0758c45a0add65e27d4b8f689482`.
- RAS native x64 ABI/layout audit is clean: Windows SDK C++ and managed probes matched all 12 checked size/offset values in green run `33164715623`; no production change was justified.

## Active deterministic work

### #94 — complete DNS message framing

Open issue: validate NSCOUNT/ARCOUNT sections and exact packet exhaustion before accepting routing evidence.

Branch: `dev/issue94-dns-complete-message`, head `525216f0c8b1470638d989affbffd6d3e0b89e17`.

First validation run `33165671844` failed before build because the transform could not find the `generic DNS section parser helper` regex anchor (`found 0`). This is validation transport only; source policy has no Windows compile/test verdict yet.

Immediate action: make the transform anchor robust against the actual post-#92 source while preserving exact-base/blob guards, then rerun. Do not weaken tests or behavior.

### #95 — unique PPP IPv4 -> Windows interface ownership

Open issue: fail closed when the RAS PPP local IPv4 maps to zero or multiple Windows adapters instead of selecting the first enumeration match.

Branch: `dev/issue95-unique-interface`, head `4e8f5e4d3a2625b76730d917b7fc293a4dc01476`.

First validation run `33165867074` applied/staged the intended two-file transform successfully, then Release compile failed with CS0019 at `VpnInterfaceResolver.cs:52`: `List<VpnInterfaceInfo> ?? VpnInterfaceInfo[]` has incompatible operands. Aggregate tests did not run.

Immediate action: fix only this type composition (for example choose an `IReadOnlyList<VpnInterfaceInfo>`-typed empty value or branch before the call), preserve unique-selection semantics/tests, rerun full Windows validation.

For both #94/#95 after green dev validation: collect bot-published source blobs, reconstruct a clean production branch on the exact then-current `main`, verify only expected source/test files, permanent Windows PR CI, rebase merge, exact-main build + handoff, lineage comment, then close completed.

## Open live issues

The nine live open issues are #2, #4, #5, #6, #7, #11, #13, #94 and #95.

#2/#4/#5/#6/#7 require real Windows 11 + real L2TP/operator evidence. #13 requires representative 12–24 h exact-binary soak evidence. #11 remains the permanent latency/throughput/memory architecture contract. Hosted Windows CI must not be described as those external acceptance results.

## Invariants not to weaken

No DIRECT fallback; outbound `Bind()` + `IP_UNICAST_IF`; custom L2TP-bound DNS; default-route/split-tunnel guards; `Disconnected -> Dialing -> Verifying -> Ready`; no usable context before real L2TP-bound HTTPS verification; exact shared/dedicated lease ownership and accepted-session drain; private CustomEphemeral PBK; DPAPI cleanup; strict HTTP/DNS grammar; pooled 32 KiB transfer path; bounded memory/concurrency; no production forced GC; unchanged 1.25x performance/security threshold policy.

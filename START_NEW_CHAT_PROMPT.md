# Start a new ProxyToAnyConnect chat

The canonical, maintained startup prompt is:

`docs/handoff/NEW_CHAT_PROMPT.md`

Copy the text after its separator (`---`) as the first message in the new chat. Before coding, the new chat must fetch live GitHub `main`, exact-head Actions and live issues/comments. Current deterministic work to resume is #94 (`dev/issue94-dns-complete-message`) and #95 (`dev/issue95-unique-interface`); live GitHub is authoritative over archived SHA snapshots.
